namespace HouseBills.Wpf.Theming;

/// <summary>The color theme the user can choose. Display names are the <c>Enum_AppTheme_*</c> resources.</summary>
public enum AppTheme
{
    /// <summary>Light or dark, following the Windows setting (also when it changes while the app runs).</summary>
    System,
    Light,
    Dark,
}